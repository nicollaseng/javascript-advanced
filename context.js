/**
 * create global execution context (global context)
 * create an object called "this" (refers to global context)
 * set up memory space for variables and functions
 * assign variables as "undefined" while placing any function declaration in memory (HOISTING)
 */
